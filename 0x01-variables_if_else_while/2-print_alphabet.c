#include <stdio.h>

/**
 * main - the entry point of the program
 *
 * Return: 0 indicates succesful program execution
 */

int main(void)
{
	char i;

	for (i = '4' ; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
